spring:
  profiles:
    # 切换环境
    active: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/sell
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
